import { request } from '../request';

/**
 * Generated API placeholder.
 * Replace endpoint and payload fields after backend contract is ready.
 */
export function fetchGet{{pascalName}}List(params: Api.{{pascalName}}.{{pascalName}}ListParams) {
  return request<Api.{{pascalName}}.{{pascalName}}List>({
    url: '/{{routeName}}/list',
    params
  });
}
