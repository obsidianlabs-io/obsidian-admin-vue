declare namespace Api {
  namespace {{pascalName}} {
    interface {{pascalName}}Base {
      {{codeKey}}: string;
      {{nameKey}}: string;
    }

    type {{pascalName}}Record = Common.CommonRecord<{{pascalName}}Base>;

    interface {{pascalName}}ListParams extends Common.CommonSearchParams {
      status?: Common.EnableStatus | null;
      keyword?: string | null;
    }

    type {{pascalName}}List = Common.PaginatingQueryRecord<{{pascalName}}Record>;

    type {{pascalName}}Payload = {{pascalName}}Base & {
      status: Common.EnableStatus;
    };
  }
}
